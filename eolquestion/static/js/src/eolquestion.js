/* Javascript for EolQuestionXBlock. */
function EolQuestionXBlock(runtime, element) {

    $(function ($) {
        /* Here's where you'd do things on page load. */
        MathJax.Hub.Queue(["Typeset", MathJax.Hub]); //Reconstruir latex
    });
}
